# Korean data sources
# DART(전자공시) — 무료 발급: https://opendart.fss.or.kr/intro/main.do
# 미설정 시 한국 종목 재무는 yfinance만 사용 (graceful fallback).
DART_API_KEY=

# LLM Providers (set the one you use)
OPENAI_API_KEY=
GOOGLE_API_KEY=
ANTHROPIC_API_KEY=
XAI_API_KEY=
DEEPSEEK_API_KEY=
DASHSCOPE_API_KEY=
DASHSCOPE_CN_API_KEY=
ZHIPU_API_KEY=
ZHIPU_CN_API_KEY=
MINIMAX_API_KEY=
MINIMAX_CN_API_KEY=
OPENROUTER_API_KEY=

# trading-ai 전용 ANTHROPIC 키 (옵션) — Hermes 통합과 분리 운영 시 사용.
# 설정되면 LangGraph 트레이딩 흐름·대시보드 LLM 의견·search_aggregator 가
# 이 키로 Claude 호출. Hermes 는 그대로 ``ANTHROPIC_API_KEY`` 를 본다 →
# 두 시스템이 서로 다른 한도·결제로 분리.
# 미설정 시 trading-ai 도 ``ANTHROPIC_API_KEY`` 로 폴백 (Hermes 와 공유).
#TRADINGAGENTS_ANTHROPIC_API_KEY=

# Optional: point at a remote Ollama server. When unset, defaults to
# the local instance at http://localhost:11434/v1. Convention follows
# the broader Ollama ecosystem; both the CLI dropdown and programmatic
# client pick this up.
#OLLAMA_BASE_URL=http://your-ollama-host:11434/v1

# Optional: LangSmith tracing. When LANGSMITH_TRACING=true and an
# API key is set, every LangChain LLM call and LangGraph node run is
# auto-traced to https://smith.langchain.com — no code change needed.
# API key 발급: https://smith.langchain.com → Settings → API Keys
#LANGSMITH_TRACING=true
#LANGSMITH_API_KEY=lsv2_pt_...
#LANGSMITH_PROJECT=trading-ai
# EU 리전을 쓰면 ENDPOINT를 EU로 바꾸세요. 기본값은 US.
#LANGSMITH_ENDPOINT=https://api.smith.langchain.com

# Optional: override DEFAULT_CONFIG without editing code.
# Any TRADINGAGENTS_* variable below, when set, replaces the matching key
# in tradingagents/default_config.py. Values are coerced to the type of
# the existing default (bool / int / str), so "true"/"3" work as expected.
#TRADINGAGENTS_LLM_PROVIDER=openai
#TRADINGAGENTS_DEEP_THINK_LLM=gpt-5.4
#TRADINGAGENTS_QUICK_THINK_LLM=gpt-5.4-mini
#TRADINGAGENTS_LLM_BACKEND_URL=
#TRADINGAGENTS_OUTPUT_LANGUAGE=English
#TRADINGAGENTS_MAX_DEBATE_ROUNDS=1
#TRADINGAGENTS_MAX_RISK_ROUNDS=1
#TRADINGAGENTS_CHECKPOINT_ENABLED=false
